export enum userType {
  GUEST,
  NORMAL,
  ADMIN,
}

export enum connectionStatus {
  CONNECTED,
  DISCONNECTED,
}

export enum roomType {
  ONEBYONE,
  MULTI,
  BOT,
}

export enum roomDetailStatus {
  JOINING,
  LEAVE,
  INVITING,
}

export enum messageStatus {
  NORMAL,
  EDITED,
}

export enum messageType {
  TEXT,
  MEDIA,
}
